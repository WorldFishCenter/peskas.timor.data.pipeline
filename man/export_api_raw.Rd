% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{export_api_raw}
\alias{export_api_raw}
\title{Export the raw trips table to the cross-country API bucket}
\usage{
export_api_raw(log_threshold = logger::DEBUG)
}
\arguments{
\item{log_threshold}{The (standard Apache logj4) log level used as a
threshold for the logging infrastructure. See \link[logger:log_levels]{logger::log_levels} for more
details.}
}
\value{
no outputs. This function is used for its side effects.
}
\description{
Projects the \strong{weighted} (pre-validation) long catch table onto the
22-column schema \verb{peskas-api-\{dev,prod\}} publishes for every country, and
uploads it to \code{conf$api$trips$raw$cloud_path}.
}
\details{
The schema was read off the live \code{peskas-api-prod} objects (2026-08-10) and is
the same 22 columns, in the same order, for Kenya, Mozambique and Zanzibar,
raw and validated alike:

\if{html}{\out{<div class="sourceCode">}}\preformatted{survey_id, trip_id, landing_date, gaul_1_code, gaul_1_name, gaul_2_code,
gaul_2_name, landing_site, n_fishers, trip_duration_hrs, gear, vessel_type,
catch_habitat, catch_outcome, n_catch, catch_taxon, scientific_name,
length_cm, catch_kg, catch_price, tot_catch_kg, tot_catch_price
}\if{html}{\out{</div>}}

Three things to know about the mapping:
\itemize{
\item \strong{Grain.} The API is one row per (trip, catch); Timor's long tables are one
row per (submission, catch, \strong{length bin}). The bins are collapsed:
\code{catch_kg} is their sum and \code{length_cm} their mean weighted by the number of
individuals counted in each bin. Empty bins are kept in the input and
contribute nothing to either, so no catch record is lost.
\item \strong{\code{trip_id}} is \verb{TRIP_<submission_id>}, as in all three other countries — a
trip identifier, not the PDS \code{tracker_trip_id}. \code{coasts::summarize_data()}
does \code{group_by(trip_id) |> slice(1)}, so it must be unique per landing, and
only half of Timor's landings match a tracked trip.
\item \strong{\code{catch_price}} is \code{NA}: Timor records revenue per landing, not per catch.
The landing's revenue is \code{tot_catch_price}, which is what Mozambique — also
without per-catch prices — publishes.
}
}
\keyword{export}
\keyword{workflow}
