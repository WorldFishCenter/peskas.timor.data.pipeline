% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{label_taxa_groups}
\alias{label_taxa_groups}
\title{Build a lookup list of taxa names grouped by fish group}
\usage{
label_taxa_groups(x)
}
\arguments{
\item{x}{A data frame/data.table with at least columns \code{catch_taxon} and
\code{fish_group}.}
}
\value{
A named list. Names are fish group labels; values are lists of unique
taxa strings belonging to each group.
}
\description{
From an input table containing \code{catch_taxon} and \code{fish_group}, constructs a
named list where each element corresponds to one \code{fish_group} and contains
the unique taxa (from \code{catch_taxon}) observed in that group.
}
\details{
Internally converts to a \code{data.table}, unique-ifies taxa within group, and
uses \code{split()} to produce the group-wise list.
}
\examples{
\dontrun{
grid <- get_file("indicators_gridded")
groups <- label_taxa_groups(grid)
names(groups)
groups[["Small pelagics"]]
}
}
\seealso{
data.table::data.table, split
}
\keyword{internal}
