% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingestion.R
\name{ingest_landings}
\alias{ingest_landings}
\title{Ingest landings surveys from KoboToolbox}
\usage{
ingest_landings(versions = c("v2", "v3"), log_threshold = logger::DEBUG)
}
\arguments{
\item{versions}{Character vector of form versions to ingest. Defaults to the
live forms, \code{c("v2", "v3")}.}

\item{log_threshold}{The (standard Apache logj4) log level used as a
threshold for the logging infrastructure. See \link[logger:log_levels]{logger::log_levels}.}
}
\value{
No output. This function is used for its side effects.
}
\description{
Downloads the live landing-survey forms from KoboToolbox, flattens each
submission into one row, and uploads the result as a versioned parquet object
to the country bucket.
}
\details{
Only the \strong{live} forms are ingested. \code{v1} (SSF Landings) received its last
submission on 2020-08-28 and is frozen: its preprocessed output was snapshot
once by \code{data-raw/freeze-landings-v1.R} and is read from
\code{surveys.landings.v1.frozen} thereafter.

The parameters needed in \code{config.yml} are:

\if{html}{\out{<div class="sourceCode">}}\preformatted{ingestion:
  landings:
    <version>:
      asset_id:
      username:
      password:
surveys:
  landings:
    <version>:
      raw:
        file_prefix:
storage:
  google:
    key:
    options:
}\if{html}{\out{</div>}}
}
\keyword{ingestion}
\keyword{workflow}
