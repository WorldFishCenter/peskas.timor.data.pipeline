% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess-metadata-tables.R
\name{preprocess_metadata_tables}
\alias{preprocess_metadata_tables}
\title{Preprocess metadata tables}
\usage{
preprocess_metadata_tables(log_threshold = logger::DEBUG)
}
\arguments{
\item{log_threshold}{The (standard Apache logj4) log level used as a threshold for the logging infrastructure. See \link[logger:log_levels]{logger::log_levels} for more details}
}
\description{
Takes the ingested Google Sheets metadata tables, parses them and writes one
versioned preprocessed list for the rest of the pipeline to read.
}
\details{
Seven tables are left, all with live readers: \code{devices} (\code{validate_imeis()}),
\code{catch_types}, \code{morphometric_table}, \code{stations}, \code{reporting_unit}, \code{habitat}
and \code{conservation}. \code{vms_installs}, \code{centro_pescas}, \code{boats},
\code{fishing_vessel_statistics} and \code{registered_boats} went in migration
Phase 11 — the frame is authoritative for the last of those
(\code{get_registered_boats()}) and the other four had no reader.

This function requires no arguments because it retrieves the parameters from
\code{conf.yml}. The fields required are:

\if{html}{\out{<div class="sourceCode">}}\preformatted{metadata:
  spreadsheet:
    name:
  version:
    preprocess:
storage:
  storage_name:
    key:
    options:
      project:
      bucket:
      service_account_key:
}\if{html}{\out{</div>}}
}
\keyword{workflow}
