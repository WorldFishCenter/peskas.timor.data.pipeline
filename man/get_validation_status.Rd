% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-functions.R
\name{get_validation_status}
\alias{get_validation_status}
\title{Read a submission's validation status from KoBoToolbox}
\usage{
get_validation_status(
  submission_id = NULL,
  asset_id = NULL,
  token = NULL,
  username = NULL,
  password = NULL,
  debug = FALSE
)
}
\arguments{
\item{submission_id}{Character. The submission to query.}

\item{asset_id}{Character. The KoBoToolbox asset id.}

\item{token}{Character. A KoBoToolbox API token, with or without the
\code{Token } prefix. Ignored when \code{username} is supplied.}

\item{username, password}{Character. Basic-auth credentials, which is how
Timor authenticates — see the note above \code{\link[=get_validation_status]{get_validation_status()}}'s
definition.}

\item{debug}{Logical. Print the request before performing it.}
}
\value{
A one-row tibble: \code{submission_id}, \code{validation_status},
\code{validated_at}, \code{validated_by}, \code{fetch_error}. A submission that has never
been validated reports \code{not_validated}; \code{fetch_error} marks a genuine
transport failure, not an absent status.
}
\description{
\code{\link[=list_validation_statuses]{list_validation_statuses()}} is the bulk form and the one the pipeline uses;
this single-submission accessor exists because
\code{\link[=update_validation_status]{update_validation_status()}} re-reads what it wrote.
}
\examples{
\dontrun{
conf <- read_config()
get_validation_status(
  submission_id = "452176760",
  asset_id = conf$ingestion$landings$v3$asset_id,
  username = conf$ingestion$landings$v3$username,
  password = conf$ingestion$landings$v3$password
)
}
}
\keyword{validation}
