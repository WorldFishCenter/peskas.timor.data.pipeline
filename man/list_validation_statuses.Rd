% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-functions.R
\name{list_validation_statuses}
\alias{list_validation_statuses}
\title{Read every submission's validation status from a KoBoToolbox asset}
\usage{
list_validation_statuses(
  asset_id = NULL,
  token = NULL,
  username = NULL,
  password = NULL,
  page_size = 1000
)
}
\arguments{
\item{asset_id}{Character. The KoBoToolbox asset id.}

\item{token}{Character. A KoBoToolbox API token, with or without the
\code{Token } prefix. Ignored when \code{username} is supplied.}

\item{username, password}{Character. Basic-auth credentials, which is how
Timor authenticates — see the note above \code{\link[=get_validation_status]{get_validation_status()}}'s
definition.}

\item{page_size}{Integer. Submissions per request. KoBoToolbox caps this at
1,000 and silently truncates anything larger.}
}
\value{
A tibble with one row per submission: \code{submission_id},
\code{validation_status} (\code{not_validated} where none is set), \code{validated_at},
\code{validated_by}, \code{fetch_error}.
}
\description{
The bulk form of \code{\link[=get_validation_status]{get_validation_status()}}, and what
\code{\link[=validate_landings]{validate_landings()}} uses. The data endpoint returns
\verb{_validation_status} alongside \verb{_id} for up to 1,000 submissions per request,
so one asset costs \code{ceiling(n / 1000)} requests rather than one request per
submission — measured 2026-08-10, 65 requests and ~90 seconds for v2's 64,997
submissions, against more than twenty minutes one at a time. It also covers
\emph{every} submission rather than only those a previous run flagged, so an
approval entered by hand on a submission the pipeline never flagged is seen
too.
}
\keyword{validation}
