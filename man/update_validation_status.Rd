% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-functions.R
\name{update_validation_status}
\alias{update_validation_status}
\title{Write a submission's validation status back to KoBoToolbox}
\usage{
update_validation_status(
  submission_id = NULL,
  asset_id = NULL,
  token = NULL,
  username = NULL,
  password = NULL,
  status = "validation_status_approved",
  debug = FALSE
)
}
\arguments{
\item{submission_id}{Character. The submission to query.}

\item{asset_id}{Character. The KoBoToolbox asset id.}

\item{token}{Character. A KoBoToolbox API token, with or without the
\code{Token } prefix. Ignored when \code{username} is supplied.}

\item{username, password}{Character. Basic-auth credentials, which is how
Timor authenticates — see the note above \code{\link[=get_validation_status]{get_validation_status()}}'s
definition.}

\item{status}{Character. One of \code{validation_status_approved},
\code{validation_status_not_approved} or \code{validation_status_on_hold}.}

\item{debug}{Logical. Print the request before performing it.}
}
\value{
A one-row tibble as \code{\link[=get_validation_status]{get_validation_status()}} returns, plus
\code{update_success}.
}
\description{
\strong{This mutates the live form.} There is no development KoBoToolbox instance,
so the two live assets are the only targets in either environment. It is
called by \code{\link[=sync_validation_status]{sync_validation_status()}} and by nothing in the recurring
pipeline.
}
\examples{
\dontrun{
conf <- read_config()
update_validation_status(
  submission_id = "452176760",
  asset_id = conf$ingestion$landings$v3$asset_id,
  username = conf$ingestion$landings$v3$username,
  password = conf$ingestion$landings$v3$password,
  status = "validation_status_approved"
)
}
}
\keyword{validation}
