% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{load_dotenv}
\alias{load_dotenv}
\title{Load environment variables from a .env file}
\usage{
load_dotenv(file = ".env")
}
\arguments{
\item{file}{Path to the \code{.env} file. Defaults to \code{.env} in the working
directory.}
}
\value{
\code{NULL}, invisibly. Called for its side effect.
}
\description{
Loads environment variables from a \code{.env} file if one exists. Called by
\code{\link[=read_config]{read_config()}} before the configuration is resolved, so that the
\verb{!expr Sys.getenv(...)} entries in \code{inst/config.yml} see the local secrets.
}
\details{
In CI the variables come from the workflow environment and no \code{.env} file is
present, so this is a no-op there. Values already set in the environment win:
\code{\link[dotenv:load_dot_env]{dotenv::load_dot_env()}} does not overwrite them.
}
\keyword{helper}
