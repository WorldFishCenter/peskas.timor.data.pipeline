% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey-reshaping.R
\name{reshape_species_groups}
\alias{reshape_species_groups}
\title{Reshape KoBo species groups into one row per catch}
\usage{
reshape_species_groups(x)
}
\arguments{
\item{x}{A raw landings table, one row per submission.}
}
\value{
A tibble keyed on \code{submission_id} and \code{n_catch}, with one column per
species-group field (\code{species}, \code{food_or_sale}, the
\verb{no_fish_by_length_group/*} counts, ...).
}
\description{
The landing forms record up to 26 catches per submission, each as a block of
\verb{species_group.<n>.species_group/<field>} columns. This turns that block
structure into one row per (submission, catch), dropping the empty catch
slots.
}
\details{
The catch index \code{n_catch} is 1-based, matching the other country pipelines.
}
\examples{
dummy <- tidyr::tibble(
  `_id` = "123",
  `species_group.0.species_group/species` = "1",
  `species_group.0.species_group/no_fish_by_length_group/no_individuals_5_10` = "3",
  `species_group.1.species_group/species` = NA_character_
)
reshape_species_groups(dummy)
}
\keyword{preprocessing}
