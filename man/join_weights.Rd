% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-taxa.R
\name{join_weights}
\alias{join_weights}
\title{Join length-weights and nutritional parameters info to preprocessed landings}
\usage{
join_weights(data, rfish_tab, nutrients_table)
}
\arguments{
\item{data}{The merged long landings table}

\item{rfish_tab}{Table with length weight parameters}

\item{nutrients_table}{Table with nutritional parameters}
}
\value{
The long landings table with \code{weight} and nutrients added
}
\description{
The function integrate nutritional info from the \code{get_nutrients_table} function
and length-weight info from FIshBase databse to preprocessed surveys data and
convert catch  labels according to the FAO nomenclature
(\url{http://www.fao.org/fishery/statistics/global-production/3/en}).
}
\section{Length types}{

\strong{Every length reaching this function is a total length.} v2 and v3 record
TL; v1 recorded fork length and was converted once, at the freeze
(done once in the freeze).
}

\section{Input and output shape}{

Both are the \strong{flat long} table — one row per (submission, catch, length
bin) — with the taxon already resolved from the assets snapshot by
\code{\link[=preprocess_landings]{preprocess_landings()}}. The output adds \code{weight} (grams) and the seven
per-catch nutrient columns and changes nothing else.
}

\keyword{preprocessing}
