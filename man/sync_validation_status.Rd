% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{sync_validation_status}
\alias{sync_validation_status}
\title{Write validation statuses back to KoBoToolbox}
\usage{
sync_validation_status(versions = c("v2", "v3"), log_threshold = logger::DEBUG)
}
\arguments{
\item{versions}{Form versions to synchronise. v1 is frozen and has no
collection.}

\item{log_threshold}{The (standard Apache logj4) log level used as a
threshold for the logging infrastructure.}
}
\value{
Invisibly, a tibble of per-submission update results.
}
\description{
Sets each submission's KoBoToolbox validation status from the flags already
pushed to the shared validation database, so the enumerators see the review
queue in the form itself. Only submissions whose status differs are patched.
}
\details{
\strong{Not part of the recurring pipeline, on purpose.} There is no development
KoBoToolbox instance, so this mutates the two live forms whatever
\code{R_CONFIG_ACTIVE} says — running it from a migration branch would change
production review state. Wire it into a workflow once that is a deliberate
decision rather than a side effect.
}
\keyword{validation}
\keyword{workflow}
