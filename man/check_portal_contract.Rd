% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portal-contract.R
\name{check_portal_contract}
\alias{check_portal_contract}
\title{Check the portal JSON contract}
\usage{
check_portal_contract(
  baseline,
  candidate,
  allow_dropped = character(),
  numbers = FALSE,
  conf = NULL
)
}
\arguments{
\item{baseline, candidate}{Each a directory of \verb{portal-*.json} files, a bucket
(\verb{gs://<bucket>} for the newest set, \verb{gs://<bucket>@<version>} to pin one by
any substring of the \verb{<timestamp>_<sha>} version — pin the \strong{full} version,
since a bare sha is shared by every run of that commit), or \code{"latest"}, the
newest set in whichever public bucket the active config resolves.}

\item{allow_dropped}{Object names whose removal is intended, so a missing
object is a note rather than a failure.}

\item{numbers}{Also print a per-column numeric summary.}

\item{conf}{The configuration file. Read when a bucket has to be resolved.}
}
\value{
A character vector of structural failures, empty when the contract
holds. The full report is printed.
}
\description{
The portal contract is \emph{discovery-based}: \code{peskas.timor.portal.v2} lists
\verb{gs://public-timor} for the \verb{portal-} prefix and keeps the newest version of
each name. A renamed object or a dropped key does not fail a build — it
silently disappears from the live site. So this asserts on \strong{names} first,
then on keys, nesting, column sets and column types.
}
\details{
Row counts and column order are reported, never failed: the portal reads an
array of objects by key, and row counts are data. Numeric summaries are
printed only when \code{numbers = TRUE}, and never fail — accepting or rejecting a
moved number needs a human.

\code{inst/tinytest/test_portal_contract.R} runs this against the frozen shape on
every pipeline run.
}
\section{Limitation}{

A bucket set is the newest version of each name, which is what the portal
sees — so if \code{\link[=export_files]{export_files()}} stopped writing an object, the previous version
stays newest and the name check cannot fire. That is why dropping
\code{indicators_grid} was harmless. Comparing two local directories catches it.
}

\keyword{export}
