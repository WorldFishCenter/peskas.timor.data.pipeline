% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pds-tracks.R
\name{describe_pds_tracks}
\alias{describe_pds_tracks}
\title{Extract per-trip descriptors from PDS tracks}
\usage{
describe_pds_tracks(log_threshold = logger::DEBUG)
}
\arguments{
\item{log_threshold}{The (standard Apache logj4) log level used as a
threshold for the logging infrastructure. See \link[logger:log_levels]{logger::log_levels} for more
details}
}
\value{
no outputs. This function is used for it's side effects
}
\description{
The one PDS product \code{coasts} has no equivalent for, and the reason Timor
still has a track-preprocessing step at all after migration Phase 7 moved
ingestion to \verb{coasts::*}. \code{coasts::preprocess_pds_tracks()} aggregates track
points into 500 m and 1 km spatial grid cells for the shared effort
products; \code{\link[=validate_pds_trips]{validate_pds_trips()}} needs something different — one row per
trip carrying the geometry and signal-quality measures its alerts are built
on:
}
\details{
\itemize{
\item \code{start_end_distance} — metres between the first and last point of a trip,
which is how a one-way trip is spotted (alert 12).
\item \code{outliers_proportion} — percentage of points faster than 30 m/s, and
\code{timetrace_dispersion} — the standard deviation of the interval between
consecutive points (together, alert 13).
\item \code{start_lat} / \code{start_lng} / \code{end_lat} / \code{end_lng}, which
\code{\link[=merge_consecutive_trips]{merge_consecutive_trips()}} needs to decide whether two trips of the same
boat are really one.
}

The step is incremental: only trips absent from the previous descriptors
artefact are read. Both workflow steps run, coasts' for the grids and this
one for the descriptors.

The parameters needed are:

\if{html}{\out{<div class="sourceCode">}}\preformatted{pds:
  pds_tracks:
    file_prefix:
    descriptors:
      file_prefix:
      version:
pds_storage:
  google:
    key:
    options:
}\if{html}{\out{</div>}}

Progress through the function is tracked using the package \emph{logger}.
}
\keyword{workflow}
