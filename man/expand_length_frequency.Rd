% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey-reshaping.R
\name{expand_length_frequency}
\alias{expand_length_frequency}
\title{Expand each catch into one row per length bin}
\usage{
expand_length_frequency(x)
}
\arguments{
\item{x}{Output of \code{\link[=reshape_species_groups]{reshape_species_groups()}}.}
}
\value{
\code{x} with the \verb{no_fish_by_length_group/*} columns replaced by one row
per bin carrying \code{length} (cm) and \code{n_individuals}.
}
\description{
The form records a count of individuals per 5 cm bin, so the length itself is
the \strong{midpoint of the bin name}, not an answer. Individuals longer than
60 cm are counted in \code{no_individuals_over60} and measured in
\code{fish_length_over60}; that recorded length replaces the 60 cm midpoint and
the measurement column is then dropped, which puts over-60 fish on the same
footing as every other bin.
}
\details{
Bins with no count are \strong{kept}. They carry a length and an \code{NA} count, they
are what the nested \code{length_frequency} column has always contained, and they
reach the portal: \code{timor_catch} is 1.7 M rows of which 93\% have no weight.
Dropping them here would change the published table.
}
\keyword{preprocessing}
