version: '3.3'
services:
  r:
    build:
      context: .
      args:
        COASTS_REF: ${COASTS_REF:-v4.6.0}
    ports:
      - "8802:8787"
    volumes:
      - .:/home/rstudio/peskas.timor.data.pipeline
    environment:
      DISABLE_AUTH: "true"
