# Parse and validate catch types table

Currently this function does not perform anything and is a placeholder

## Usage

``` r
pt_validate_catch_types(catch_type_table)
```

## Arguments

- catch_type_table:

  a data frame with the catch type info

## Value

a tibble
