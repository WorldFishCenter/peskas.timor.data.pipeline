# Estimate distance between consecutive trips

This function calculates the geographical distance in meters between the
end and the start of two consecutive trips.

## Usage

``` r
get_distance(x)
```

## Arguments

- x:

  A data frame containing trips coordinates.

## Value

A vector of distances in meters
